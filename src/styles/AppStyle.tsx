import { makeStyles } from "@material-ui/core/styles";

const AppStyle = makeStyles((theme) => ({
  leftPanelMainGrid: {
    backgroundColor: "#5948EA",
    height: "100vh",
  },
}));

export default AppStyle;
