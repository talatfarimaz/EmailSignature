import { makeStyles } from "@material-ui/core/styles";

const LeftPanelStyle = makeStyles((theme) => ({
  selectedTab: {
    backgroundColor: "#60C869",
    borderRadius: "15px",
    width: "20%",
  },
}));

export default LeftPanelStyle;
